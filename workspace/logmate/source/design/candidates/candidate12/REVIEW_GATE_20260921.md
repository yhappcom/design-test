# Home Candidate 12 — Design Studio Gate

Status: HOLD — implementation correction required before render.
Source checkpoint: `f992d62a98193629d19346a022a64deef824570c`
Candidate implementation commit reviewed: `20dbf9016169dc3fb29b14a7df3d4460f5a31d37`
Design Studio evidence snapshot read: `main` tree `1d0ebf27e498091ec926bbfb61165acbf1ed1760`; synthesis `STATIC_RESEARCH_SYNTHESIS_EXTERNAL_ADVISORY_20260921.md`.

## Scope integrity
Candidate 12 was opened independently from the required structural checkpoint. Candidate 01 and rejected Candidates 07–11 were not used as generation inputs. `HomeV011` is retained as the checkpoint structural implementation, so Home order, fixtures, semantics, action placement and operational-data geometry are unchanged.

## Independent discipline passes — R1

### Type — REVISE
Evidence: Type T084 runtime transfer principles and current synthesis: requested/resolved family, fallback, scaling and product-string geometry are part of the rendering system. The candidate wrapper supplies no distinct type character because `HomeV011` internally rebuilds its own theme and inherits only the wrapper body family, which is unchanged. A visual candidate may keep mature platform typography, but the intended concept is not actually expressed at exact implementation level. Verdict: REVISE.

### Color — REVISE
Evidence: Color C115 and current synthesis require semantic rank and light/dark transfer. The wrapper palette does not survive because `HomeV011` replaces canvas/surface/accent with `_V11Theme`/`_V11Colors`. Candidate 12 therefore renders baseline V11 colors, not the declared aviation-paper palette. Verdict: REVISE.

### Layout / Spatial — KEEP
Evidence: L106/L105 and Operational Geometry Contract. Candidate imports the checkpoint Home implementation without changing widget order, spacing, grid flexes, touch geometry, route zoning or metric geometry. No structural/layout mutation is introduced. Verdict: KEEP for R1 implementation.

### Interaction — KEEP
Evidence: I102 and current synthesis authority/state rules. Existing semantics, hit targets, focus ownership, selector state and action loci are unchanged. The wrapper introduces no false persistence or outcome state. Verdict: KEEP for R1 implementation.

### Content Design — KEEP
Evidence: CD121 and current synthesis semantic architecture. Exact product-authored strings and action/destination wording remain unchanged. No semantic abbreviation or new status claim is introduced. Verdict: KEEP for R1 implementation.

### Web / Runtime — REVISE
Evidence: W115/W114 and current synthesis provenance ladder. Exact-source review shows the visual override is shadowed by the nested Home theme; therefore the implementation does not realize its declared visual concept. Rendering this revision would produce misleading evidence. Verdict: REVISE.

### Cross-cutting accessibility / UX integration — KEEP WITHIN STATIC SCOPE
Accessibility is cross-cutting rather than a sixth canonical specialist. The revision does not remove semantics, reduce existing target geometry, rely on new color-only state, alter protected operational relationships, or claim human/AT validation. Runtime/human closure remains explicitly unclaimed. Static verdict: KEEP.

## Gate result
Unanimous KEEP is not achieved (Type REVISE, Color REVISE, Web/Runtime REVISE). Rendering is prohibited. Required correction: implement Candidate 12 visual tokens inside the exact Home implementation rather than in an outer theme that is shadowed, while preserving frozen structure and geometry. After correction, rerun every discipline from scratch.
